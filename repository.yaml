# L'archivio degli add-on di gdahome.
#
# E' questo file che rende questa repository un **archivio di add-on**: chi
# incolla il suo indirizzo in Home Assistant — Impostazioni → Add-on →
# Negozio → i tre puntini → Archivi — si trova «Il ponte» fra gli add-on da
# installare, e da li' in poi gli aggiornamenti gli arrivano come per ogni
# altro add-on.
#
# Il Supervisor guarda questo file e le cartelle di primo livello che hanno un
# `config.yaml`: oggi una sola, `ponte/`. Non serve nessun gettone, perche'
# questa repository e' pubblica.
name: gdahome
url: https://github.com/danigio15/gdahomeapp
maintainer: danigio15 <https://github.com/danigio15>
